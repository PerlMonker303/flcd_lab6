START
DEF A:NUMBER; B:NUMBER; MESSAGE:STRING
ASSIGN MESSAGE : "The greatest common divisor between "
READ A
READ B
PROC GCD(A:NUMBER; B:NUMBER)
STARTPROC
    IF A === B STARTIF RETURN A FINISHIF
    IF B > A STARTIF RETURN CALL GCD(A, B-A) FINISHIF
    RETURN CALL GCD(B, A-B)
FINISHPROC
PRINT(MESSAGE + A + " and " + B + " is " + CALL GCD(A , B))
FINISH
